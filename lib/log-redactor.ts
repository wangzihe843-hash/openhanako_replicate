import redactor, {
  redactLogText,
  redactLogValue,
  redactLogLabel,
  formatLogArgs,
} from "../shared/log-redactor.ts";

export {
  redactLogText,
  redactLogValue,
  redactLogLabel,
  formatLogArgs,
};

export default redactor;
