# 2026-09-21 warning 后续排查与修复

承接 [上一轮修复报告](2026-09-21-warning-fixes.md)。用户要求先确认最终失败是否由刚才的修改引入，再修复根因，并由 subagents 再次分块审查、修复其他确认问题。

最终验证通过：剩余阻塞已修复，分块及交叉审查发现的确认问题均已处理。完整测试退出 0，进入用户已授权的 commit / push；远端结果以最终交付消息为准。

## 剩余失败的归因

角色面板、profile 存储和原失败测试在本轮开始时均与 HEAD `fa004739011be91932a58a03e263f2eaa6a7e8f7` 一致。主任务增加可控的旧 GET 与新 PUT 顺序测试，在修改产品代码前稳定复现：用户选择档位基线后，旧资料响应把确认条恢复。

从 HEAD 导出的隔离源码副本运行同一个新增用例，仍在同一“不应存在确认条”断言失败。故这条真实竞态早于上一轮修复，不能归因于刚才的工作流、媒体、草稿或插件补丁。原全量测试的具体调度未被记录，因此不声称还原了它的全部 Promise 顺序；本次修复覆盖了同一现象的确定性触发路径。

同时，两个 `saveXingyeRoleProfile` 并行调用可以读到同一旧快照，后写者抹去先写者的不同字段；新增回归在未修改的原存储代码上先失败。现按 renderer 中的连接和角色顺序执行读、合并、写；不同角色或连接仍独立，前一保存失败不会阻断后续保存。该队列不承诺跨进程或跨客户端事务。

## 新一轮修复

| 编号 | 确认的问题与修复 | 验证 |
| --- | --- | --- |
| R10 | 旧 profile 刷新可恢复已经选择完的黑化确认条。增加本地选择与保存回执保护；工坊即时状态在 await 前设置，完成后的旧回调不再覆盖后续选择；失败时恢复已确认字段。 | HEAD 隔离对照先红；覆盖旧 GET、新提案、失败→主保存、工坊保存途中采用/拒绝及最终落库和关系状态。 |
| R11 | 同角色并行保存不同字段相互覆盖。按连接、角色串行读合并写，捕获调用时 patch，失败后继续队列。 | 并行保存先红；另验证失败续队及不同角色不阻塞。 |
| R12 | 懒加载插件激活超时绕过启动清理，留下部分工具与 activating 状态。仅清理所属加载代次，标记失败，保护重新启用后的新实例。 | 实际 PluginManager 超时和 disable/re-enable 迟到失败回归。 |
| R13 | 上一轮 R04 的预算处理遇到装不下的超长标题就退出，后续正常 Lore 也丢失。改为跳过该块，保留完整标记及预算限制。 | 合法超长标题在 HEAD helper 保留后续正文、R04 初稿丢失、补丁恢复；同步写入及两种旧文件读取路径。 |
| R14 | 增加 model-mode 局部默认值后，上层 provider-mode 默认值整组丢失。改为逐字段继承。 | 真实 HTTP 保存后 image/video 解析：watermark 和 duration 均保留正确继承值。 |
| R15 | 旧 mode.options 的值压过界面编辑，显示也与生成使用值不符。统一读取优先级，编辑/清空只移除对应旧键，保留兄弟配置。 | React 到真实管理器/存储联合用例，新增媒体 4 项先红后绿。 |
| R16 | 新草稿尚未 PUT 成功时归档，清除 runtime 缓存后恢复会读回旧服务端草稿。保留未确认输入快照，按连接/surface/会话与版本隔离；确认且旧 GET 完成后释放正文。 | 原复现先红；新增 9 项覆盖 PUT/GET 交错和失败、clear、path/id、连接及 surface，草稿套件 31 项。 |

R13 是已证实的上一轮修复回归，已纠正。R12 是原有懒激活路径遗漏；R14–R16 是继续检查相邻行为时确认的问题。没有把“测试曾通过”当作排除缺陷的依据。

## 交叉复审与验证

三个 subagents 交换分块，主任务处理角色失败；完成后再次交叉审阅。交叉复核发现“采用精确值保存失败后残留内存值”，主任务补用例复现并修复，现失败后再次主保存仍保留待确认提案，不会写入失败的精确值。

定向测试有重叠，不累计为独立覆盖数：角色/profile 2 文件 45 项；媒体 6 文件 108 项；工作流/媒体轮询 7 文件 146 项；插件 3 文件 151 项；Lore/计数/事件 5 文件 77 项；草稿/启动/会话 7 文件 175 项。结果均为真实进程退出 0。

| 最终统一检查 | 结果 |
| --- | --- |
| 三套 TypeScript | npm run typecheck 通过，exit 0。 |
| 全仓 ESLint warning 门禁 | 0 errors、7,881 warnings；相对本轮起点新增 0、移除 0；相对原审查净减少 7。 |
| Renderer / server bundle | 两者生产构建均通过；沿用前轮已披露的 chunk / mixed import / chokidar 提示。 |
| 持久化兼容性与运行时闭包 | 62 stores / 866 sites，DATA_EPOCH 1；固定 payload 660bd9fe30c878dd1647c36d8a78152e19181f2334970a617de3018c86973afd 已正式核验。9,690 个闭包文件及开放边界均不变。 |
| 完整默认测试 | exit 0；1,365 文件通过 / 2 文件跳过；14,501 项通过 / 41 项跳过 / 0 失败，0 未处理异常；耗时 729.82 秒。 |
| 测试清单、源码快照、Git 差异 | 实际执行与正式清单均为 1,367 文件，无遗漏/额外文件；全量前后 3,054 个源码/config 摘要相同；git diff --check 通过。 |

最终全量仍使用仓库外的隔离 HANA_HOME、用户目录与 TEMP；不调用真实模型或媒体服务。原始证据在忽略目录 `.cache/fixes-20260921/`，其中 `role-race-head-complete.log` 是 HEAD 对照，`role-race-before.log`、`profile-save-race-before.log`、`failed-accept-before.log` 保留修复前失败。后续最终结果独立保存为 `followup-*`，不会覆盖前轮失败日志。

## 保留边界

- 未消除全仓既有 warning；不提高阈值、不引入规则豁免，不靠重复跑到通过来忽略失败。
- 未确认草稿只驻留当前 renderer 内存；失败 PUT 沿用下次编辑重试，未落盘输入不承诺关闭或重启后恢复。
- profile 保存队列只约束同 renderer 内的调用，不新增跨客户端事务或磁盘格式。
- 保留前轮已披露的 Lore/事件历史迁移、构建 chunk 提示及未执行签名安装包/真实 provider 验证等限制。
